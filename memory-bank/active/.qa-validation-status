PASS

QA semantic review of windows-split-scope (Level 1), baseline memory-bank/active/projectbrief.md.

Verified:
- Requirements 1-4 and acceptance criteria 1-3 all met by `path_slug` / `repo_slug` in `memo` and the ntpath-based cases in `test.py`.
- `python3 test.py` re-run by QA: 109118 passed, 0 failed.
- Build diff (06ade95..32b906b) is scoped to the two slug helpers, `scope_dir` delegation, and tests; no debris, no TODOs, no debug artifacts.
- POSIX behavior pinned by regression checks (owner/repo remotes, POSIX path slug); README makes no platform claims, so no doc update was owed.

Advisories (non-blocking):
- UNC-drive branch in `path_slug` goes beyond the brief's C:/S: cases, but it prevents a silent store-identity collision (different shares would otherwise merge), which is the system's core invariant. Keep.
- `repo_slug` on an owner-less Windows path (e.g. C:\widget.git) uses the drive letter as "owner" (repo/c/widget). Harmless: still nests under optmem, which is all the brief requires.
